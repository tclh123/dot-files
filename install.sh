#!/usr/bin/env bash

# set -e
#  
# here="$(cd "${0%/*}"; pwd)"
#  
# for file in "$here"/*; do
#   name="$(basename "$file")"
#   [[ $name = bin ]] && dotname="$name" || dotname=".${name}"
#  
#   if ! [[ "install readme" =~ $name || $name =~ ".plist" || -e ~/$dotname || -d $file/.git ]]; then
#     ln -sfhv ${file#$HOME/} "${HOME}/${dotname}"
#   fi
# done


# ln -sfnv dot-files/dot-vim $HOME/.vim

# ln -sfnv dot-files/dot-vim/vimrc $HOME/.vimrc
